export component AppWindow inherits Window {
    title: "MySlintOverlay";
    width: 400px;
    height: 300px;
    background: white;
    always-on-top: true;
    Rectangle {
        width: 100%;
        height: 100%;
        background: rgba(255, 0, 0, 0.5);
        Text {
            text: "Click-through Overlay";
            color: white;
            font-size: 20px;
        }
    }
}
